# Mandatory fields
odp_implementation = "linux-generic"
config_file_version = "0.1.11"

# Global init options
global_init: {
	# Disable unused features.
	disable: {
		compress    = 1
		crypto      = 1
		ipsec       = 1
		traffic_mng = 1
	}
}
